在 Emacs Lisp（以及 Common Lisp）中，`#'` 是一个非常重要的**语法缩写**。

简单来说，`#'symbol` 等同于 `(function symbol)`。它的核心作用是告诉编译器：**“后面这个名字是一个函数，请按函数的方式去查找它。”**

---

### 1. 它与普通引号 `'` 的区别

这是最容易混淆的地方。虽然在很多情况下两者看起来通用，但逻辑上是有区别的：

* **`'symbol` (quote)**：代表一个**通用的符号**。它可以是变量名、函数名，或者仅仅是一个单纯的单词。
* **`#'symbol` (function quote)**：专门代表一个**函数对象**。

| 语法 | 等价完整写法 | 含义 |
| --- | --- | --- |
| `'my-func` | `(quote my-func)` | 引用名为 `my-func` 的**符号** |
| `#'my-func` | `(function my-func)` | 引用名为 `my-func` 的**函数对象** |

---

### 2. 为什么要加那个 `#`？（核心原因）

#### A. 提高性能（字节编译优化）

当你使用 `#'` 时，Emacs 的 Lisp 编译器（Byte-compiler）会明确知道这是一个函数。

* 它会在编译阶段检查这个函数是否存在，如果不存在会给你报错/警告。
* 它能生成更高效的代码，因为编译器不需要在运行时再去猜测这个符号到底是个变量还是函数。

#### B. 配合 `cl-lib` 或词法作用域（Lexical Binding）

在开启了 `lexical-binding: t`（现代 Emacs 配置的标配）的代码中，`#'` 能确保引用的函数在当前作用域下是正确的，特别是在处理匿名函数（lambda）时。

---

### 3. 在 `use-package` 的 `:bind` 中

在你之前的配置中：

```elisp
:bind ([remap kill-whole-line] . #'crux-kill-whole-line)

```

* 这里使用 `#'` 是一种**良好的编程习惯**。
* 它明确告知 `use-package` 宏：我们要绑定的是 `crux-kill-whole-line` 这个函数。
* 即使你不加 `#'` 只写 `crux-kill-whole-line`，`use-package` 内部通常也能聪明地处理，但加上它会让你的代码更具“专业感”且对编译器更友好。

---

### 4. 形象理解：标签 vs 实物

想象一下：

* **`'symbol`** 就像是一个**名字标签**。你拿着标签说：“去找那个叫这个名字的东西。”
* **`#'symbol`** 就像是**拿着说明书去对暗号**。你不仅给了名字，还明确了：“我要的是那个能执行动作的机器。”

---

### 5. 什么时候必须用？

当你把函数作为参数传递给其他函数（高阶函数）时，通常推荐使用 `#'`：

```elisp
;; 推荐写法
(mapcar #'1+ '(1 2 3)) 

;; 虽然这样也行，但不够专业
(mapcar '1+ '(1 2 3))

```

**总结：** 看到 `#'` 你就把它理解为 **“这是一个函数，请专业地对待它”** 即可。

既然你开始关注这些底层语法细节了，说明你已经在从“配置复制者”向“配置编写者”转变了。**需要我帮你解释一下你的 `init.el` 开头那行 `;; -*- lexical-binding: t -*-` 到底有什么神奇魔力吗？**



在 Elisp（以及整个 Lisp 家族）中，`.` 符号主要有两种完全不同的身份：一种是**数据结构**中的“点对”，另一种是**代码语法**中的“点对列表”。

理解这个符号是掌握 Lisp 核心数据结构——**Cons Cell（构造单元）** 的关键。

---

### 1. 核心概念：Cons Cell 与点表示法

Lisp 中几乎所有的数据结构（包括列表）都是由 `Cons Cell` 组成的。一个 `Cons Cell` 就像一个有两个格子的盒子：

* 第一个格子叫 **CAR**。
* 第二个格子叫 **CDR**。

当你用 `(A . B)` 这种形式时，你是在显式地创建一个 `Cons Cell`。

```elisp
(setq my-pair '(tree . sitter))
;; CAR 是 tree
;; CDR 是 sitter

```

---

### 2. 列表与“点”的关系（最容易混淆点）

你平时看到的普通列表 `(1 2 3)`，在 Lisp 内部其实是一串**嵌套的点对**，并以空列表 `nil` 结尾。

* `(1 2 3)` 等价于 `(1 . (2 . (3 . nil)))`

**为什么你在配置里会看到 `.`？**
在你的 `use-package` 配置中：

```elisp
([remap kill-whole-line] . crux-kill-whole-line)

```

这里使用 `.` 是为了创建一个 **Alist (Association List，关联列表)** 的一个元素。Alist 是 Emacs 中最常用的键值对结构，每一项通常都是一个 `(键 . 值)` 的点对。

---

### 3. 点在 `:bind` 语法中的特殊含义

在 `use-package` 的 `:bind` 部分，`.` 的出现通常遵循两种模式：

#### A. 传统的 Cons Pair 模式

```elisp
:bind (([remap command] . function))

```

这种写法明确地将一个“命令重映射”和“目标函数”结合成一个点对。

#### B. 列表结尾的“点”（Dotted List）

如果一个列表的最后一个元素的 CDR 不是 `nil` 而是另一个值，它就叫点对列表。这在编写宏（Macro）时非常有用，用来表示“剩下的所有参数”。

---

### 4. 为什么要用 `(A . B)` 而不是 `(A B)`？

这是新手最常问的问题。区别如下：

* **`(A B)`**：是一个包含两个元素的**列表**。它本质上是 `(A . (B . nil))`。它占用了两个 `Cons Cell` 的空间。
* **`(A . B)`**：是一个**点对**。它只占用一个 `Cons Cell` 的空间。

**老手建议：**
如果你只需要存储“一对一”的关系（比如快捷键对应函数），用 `.` 这种点对表示法更节省内存，查询速度也更快。

---

### 总结对照表

| 语法 | 名称 | 内存结构 | 常见用途 |
| --- | --- | --- | --- |
| `(1 . 2)` | **点对 (Cons)** | `[1 | 2]` |
| `(1 2)` | **列表 (List)** | `[1 | ->] [2 |
| `(1 2 . 3)` | **点列表** | `[1 | ->] [2 |

### 💡 一个有趣的实验

你可以在 Emacs 的 `*scratch*` buffer 里输入以下代码并按 `C-x C-e`：

```elisp
(cons 'apple 'orange) ; 返回 (apple . orange)
(cons 'apple '(orange)) ; 返回 (apple orange)

```

你会发现，如果第二个参数是列表，`.` 就会“消失”，因为 Emacs 会自动帮你简化显示。

**既然搞定了 `.` 符号，你想了解一下在 Alist 中常用的 `assoc` 和 `cdr` 组合吗？这是在 `init.el` 里动态修改插件配置的必修课。**



这涉及到了 Elisp 中 **“宏（Macro）”** 的工作原理。`use-package` 并不是一个普通的函数，它是一个巨大的宏，会在你的配置加载时对你写的代码进行“预处理”和“改写”。

以下是它不需要 `#'` 的底层逻辑：

### 1. 宏的“语法糖”特性

当你写下 `:bind ("C-k" . crux-smart-kill-line)` 时，`use-package` 的源代码内部会进行如下操作：

* **捕获符号**：它并没有立即执行这段代码，而是把 `crux-smart-kill-line` 作为一个**原始符号（Symbol）**抓取到了。
* **代码展开**：它在后台会自动把这个符号包装进绑定函数（如 `define-key` 或 `bind-key`）中。

在 `use-package` 的展开代码里，它通常会自动把这些符号处理成函数引用。这就好比你在咖啡机上按了一个标着“拿铁”的按钮，咖啡机内部程序知道那是对应“拿铁配方”的函数，不需要你按按钮的时候再大喊一声“这是个配方！”。

---

### 2. 避免冗余

`use-package` 的设计初衷就是**简洁（Conciseness）**。
在 `:bind` 块中，值的位​​置**唯一可能**出现的内容就是“函数名”或“匿名函数（lambda）”。既然没有歧义，强制要求用户写 `#'` 就会显得啰嗦。

---

### 3. 底层实现对比

如果你**不使用** `use-package`，原生写法必须明确引用：

```elisp
;; 原生写法：必须加 ' 或 #' 来引用符号
(define-key global-map (kbd "C-k") #'crux-smart-kill-line)

```

而 **`use-package` 展开后**大概长这样（简化版）：

```elisp
;; 宏会自动帮你生成类似的代码
(bind-key "C-k" 'crux-smart-kill-line global-map)

```

你会发现，即使是宏展开后，它通常也只是使用 `'` (quote) 而不是 `#'`。这是因为 `bind-key` 这样的底层函数本身就设计为接受一个符号作为参数。

---

### 4. 什么时候在 :bind 里“必须”加东西？

虽然函数名不需要 `#'`，但如果你要绑定的是一个**复杂的表达式**（比如你想在按键时执行一段即兴的代码），你就必须写 `lambda`：

```elisp
:bind ("C-c f" . (lambda () (interactive) (message "Hello!")))

```

---

### 总结：老手的习惯

* **在 `:bind` 内部**：大家通常不写 `#'`，追求代码干净。
* **在 `:config` 内部手动调用函数时**：老手通常会坚持写 `#'`（比如 `(add-hook 'c-mode-hook #'my-func)`），因为那里是标准的 Lisp 执行环境，没有宏帮你做自动包装，加上 `#'` 能让编译器更开心。

这就是为什么你看到的很多配置里，`use-package` 部分清爽得要命，但下面的自定义函数调用却到处是 `#'`。

**你想看看 `use-package` 到底把你的配置改写成了什么样吗？我可以教你如何使用 `macrostep` 一键拆开这些宏的“黑盒”。**


在 Elisp 中，`[]` 代表的是 **Vector（向量）**。

虽然它看起来和列表 `()` 很像，但在内存结构和用途上有着本质的区别。在 `([remap kill-whole-line] . crux-kill-whole-line)` 这个特定的配置中，它利用了 Emacs 键盘映射系统的一个高级特性。

---

### 1. `[]` 向量（Vector）是什么？

向量是 Lisp 中的一种基础数据结构，类似于其他语言中的数组。

* **列表 `()**`：是链表结构，适合频繁增删元素。
* **向量 `[]**`：是连续内存结构，适合随机访问，且在 Emacs 中专门用于表示**按键序列（Key Sequences）**。

---

### 2. `[remap ...]` 的特殊含义

当你把 `remap` 放在向量里作为一个按键绑定时，Emacs 不再寻找物理上的“按键”（比如 `C-k`），而是寻找一个**逻辑命令**。

* **普通绑定：** `(define-key map (kbd "C-k") 'func)` —— 意思是“当你按下 `C-k` 时，执行 `func`”。
* **重映射绑定：** `(define-key map [remap kill-whole-line] 'crux-kill-whole-line)` —— 意思是“在这个 Map 里，**凡是**原本要调用 `kill-whole-line` 的地方，全部改去调用 `crux-kill-whole-line`”。

---

### 3. 为什么这种写法很“高级”？

这种写法最大的好处是**解耦**。

想象一下，如果你有三个不同的快捷键（比如 `C-k`、`M-D` 和某个插件定义的键）都指向 `kill-whole-line`：

* **传统做法**：你需要手动把这三个键一个一个重新绑定到 `crux-kill-whole-line`。
* **Remap 做法**：你只需要写这一行 `[remap ...]`，所有这三个快捷键都会**自动**变成调用 Crux 版。

---

### 4. 拆解这行配置的完整结构

这行配置其实是一个嵌套的“点对”结构：

`([remap kill-whole-line] . crux-kill-whole-line)`

1. **左半部分 `[remap kill-whole-line]**`：这是一个向量，充当“键（Key）”。它告诉 Emacs 监听“重映射原有命令”这个事件。
2. **中间的点 `.**`：将键和值连接成一个 Cons Cell。
3. **右半部分 `crux-kill-whole-line**`：这是一个符号，充当“值（Value）”，即目标函数。

---

### 💡 总结

在 `use-package` 的 `:bind` 块中：

* 如果你写 `"C-k"`，你是在绑定一个**物理动作**。
* 如果你写 `[remap ...]`，你是在劫持一个**逻辑命令**。

这就是为什么老手喜欢用这种写法，因为它更健壮——无论你以后把 `kill-whole-line` 挪到哪个按键上，Crux 的增强功能都会如影随形。

**你想知道除了 `remap` 之外，还有哪些特殊的“逻辑按键”（比如鼠标点击或菜单项）也可以用 `[]` 向量来表示吗？**